package questions;

public class ExceptionHandling {
    public void catchWrongly() {
//        try {
//            throw new NumberFormatException();
//        } catch (RuntimeException e) { // keep the hierarchy catches, from most precise (child exception) to most
//                                       // broad one (parent exception)
//
//        } catch (IllegalArgumentException e) {}
    }
}
